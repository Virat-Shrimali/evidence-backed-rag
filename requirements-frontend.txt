# Frontend-only dependencies for Streamlit deployment
# Zero neural model / PyTorch / vector DB dependencies to ensure ultra-fast builds and low memory footprint.
streamlit>=1.41.0,<2.0.0
httpx>=0.28.0,<1.0.0
pydantic>=2.10.0,<3.0.0
